/**
 * expects an url relative to the index.html directory and load the requested file
 */
export default class Loader {
    constructor(url) {
        this.url = url;
    }
    async fetch() {
        //await fetch()
    }
    get() { }
}
